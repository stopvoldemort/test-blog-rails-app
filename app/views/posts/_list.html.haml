

%table
  %thead
    %tr
      %th Title
      %th Author
      %th Rating
  %tbody
    - Post.sorted_by_rating(posts).each do |post|
      %tr
        %td= link_to post.title, post |
        %td= "by #{post.author.full_name}"
        %td= post.rating_string
